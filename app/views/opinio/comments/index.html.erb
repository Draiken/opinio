<h1>Comentários de <%= @commentable.universal_name %></h1>

<%= paginate @comments %>

<%= render :partial => "comments/comments", :locals => { :comments => @comments } %>

<%= paginate @comments %>
